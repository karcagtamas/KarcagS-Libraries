@inject NavigationManager Navigation

@code {
    [Parameter]
    public string Path { get; set; } = "/not-found";

    protected override void OnInitialized()
    {
        base.OnInitialized();

        Navigation.NavigateTo(Path);
    }
}
